type EasingFunction = [number, number, number, number];

// Mirrors --ease-snappy in src/styles.css; motion needs the array form.
export const EASE_SNAPPY: EasingFunction = [0.2, 0.4, 0.1, 0.95];

export const EASE_OUT: EasingFunction = [0.23, 1, 0.32, 1];
